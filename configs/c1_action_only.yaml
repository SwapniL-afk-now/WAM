# C1: action-only NFT. Same rollouts (one-step imagine-then-act), imagination frozen.
defaults:
  - libero_rl_zeroshot
  - _self_
runner:
  logger:
    experiment_name: "c1_action_only"
algorithm:
  imago_video_weight: 0.0
actor:
  model:
    imago:
      action_grad_through_video: false
    imago_trainable:
      video:
        mode: frozen
