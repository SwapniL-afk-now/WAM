# Realism-anchor sweep point (main run uses imago_beta_real 0.0).
defaults:
  - libero_rl_zeroshot
  - _self_
runner:
  logger:
    experiment_name: "imago_beta_real_0p1"
algorithm:
  imago_beta_real: 0.1
